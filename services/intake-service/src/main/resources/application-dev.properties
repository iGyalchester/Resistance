#
# dev: everything local, nothing external required.
# - MySQL from infrastructure/docker-compose.yml (localhost:3306)
# - webhook open (no token), SNS topic check off, IMAP off
# - PII encryption off (plaintext) - EncryptionConfig logs a warning
#
# allow unsigned curl posts to /intake/aws-sns locally
intake.aws.verify-signature=false

logging.level.com.resistance=debug
